# SUBMIT — G2 Digital Markets (one form → Capterra + GetApp + Software Advice)

**Status:** 🟡 **READY FOR OPERATOR** (ENTITY-FOOTPRINT-W1 R4). Free on all three sites. ~15 min submit + decision 1–3 business days; full publication can take **days–weeks**.
**Submitter:** Mr.1. **One submission propagates to all three** (G2 acquired the Gartner "Digital Markets" trio; sale closed 2026-02-05).
**Portal:** <https://app.g2digitalmarkets.com/get-listed/start> · contact `listings@g2digitalmarkets.com`.

> ℹ️ This is **separate** from the G2.com listing (`docs/SUBMIT_G2.md`). Do both. G2DM accepts **B2B or B2C** but requires a **public sales CTA** (no waitlist-only). Beta products are allowed **only if ≥1 genuine review lands within the first calendar year** — plan one real-user review ask (see below).

---

## Why G2DM (probe results, 2026-06-05)
- **Three surfaces for one form:** the submission propagates to **Capterra + GetApp + Software Advice** — high-authority B2B software directories that LLM assistants cite for category queries.
- **Free listing on all three;** the content team edits final copy at its discretion and may have **auto-created** a listing already → check first and **claim** rather than create.

## Pre-flight (confirm before starting)
1. **Search all three for an existing/auto-created listing** and claim if found:
   - Capterra <https://www.capterra.com/search/?query=AlgoVault>
   - GetApp <https://www.getapp.com/search/?q=AlgoVault>
   - Software Advice <https://www.softwareadvice.com/search/?q=AlgoVault>
2. **Public sales CTA** is live (pricing + free-tier signup at algovault.com) — required; a waitlist-only page is rejected.
3. **Save 2–3 competitor listing URLs** (comparable B2B crypto/AI/API tools) as **category-appeal ammunition** — category mismatch is rejection reason #1.
4. **One primary category** only at submit (you can refine later).

## Field set (UNVERIFIED — JS-gated form; confirm exact labels at execution)
Product name `AlgoVault` · website `https://algovault.com` · ONE primary category · the description below · **named contact + company email** (`admin@algovault.com` or a person-named alias). The exact field list is behind the logged-in JS form — treat the above as the expected set and adjust at submit time.

---

## Approved description (paste-ready — third-person, 150–250 words, no superlatives, no CTAs, no links, no company suffixes)

```
AlgoVault is an MCP (Model Context Protocol) server and API for AI trading agents. It returns a single composite trade call — direction, confidence, and market regime — by combining multiple quantitative factors across cryptocurrency perpetual-futures venues, instead of exposing raw indicators for a person to read. The same API provides market-regime classification and cross-venue funding-rate comparison in one call.

The service is designed for autonomous software clients rather than human chart-watchers. Agents connect as a remote hosted service, as a local stdio process, or through Model Context Protocol registries. Each response is structured for direct use inside an agent's decision loop.

The published track record is independently verifiable. Every trade call is recorded on-chain, Merkle-anchored on the Base network, so outcomes can be checked rather than taken on trust.

AlgoVault provides a free tier with a monthly call allowance and usage-based paid plans for higher volumes; per-call micropayments are also supported. It targets developers building AI trading agents, algorithmic-trading systems, and Model Context Protocol clients. Coverage spans multiple cryptocurrency derivatives venues and a range of assets and timeframes.
```

**Style rules (G2DM editorial — the description above already complies):** third-person only — NEVER first-person ("we"/"our"). NEVER use superlatives — avoid `best`, `leading`, `revolutionary`, `cutting-edge`, `powerful`, world-class. NEVER include URLs, phone numbers, CTAs, or `Inc`/`LLC` suffixes. The content team rewrites at its discretion, so keep claims plain and verifiable.

---

## Click-by-click (Mr.1)
1. Open **<https://app.g2digitalmarkets.com/get-listed/start>**.
2. Enter product name **`AlgoVault`**, URL `https://algovault.com`, **one** primary category (pre-flight #3 list), the description above, named contact + company email.
3. Submit → decision in **1–3 business days**; full publication to the three sites can take **days–weeks**.
4. In the G2DM dashboard, **verify the listing propagated** to Capterra, GetApp, AND Software Advice.
5. Tell Code the live Capterra URL → Code flips `capterra` in `landing/_jsonld/entity-urls.json` + re-runs the generator. (GetApp/SA share the propagation; Capterra is the canonical `sameAs` entry.)

## Beta-rule review ask (within the first calendar year — required to keep the listing)
- Plan **one genuine review** from a real user (a Telegram-bot subscriber who actually uses it), neutral ask, good-or-bad. NEVER incentivize or off-platform-farm reviews.

## Pushback to expect
- **Category-rejected (most common):** appeal to `listings@g2digitalmarkets.com` with the 2–3 competitor listing URLs from pre-flight #3.
- **Copy edited:** the content team may rewrite the description; that is expected and fine.
- **Already listed:** if an auto-created listing exists, claim it instead of creating a duplicate.
