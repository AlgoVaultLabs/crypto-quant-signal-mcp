# SUBMIT — G2.com (free product listing; B2B AI-recommendation surface)

**Status:** 🟡 **READY FOR OPERATOR** (ENTITY-FOOTPRINT-W1 R3). Free tier. ~25 min submit + **3–5 business-day** research-team vetting.
**Submitter:** Mr.1. **Product name on G2 must be exactly `AlgoVault`** (as on the site — no marketing words).
**Portal:** <https://www.g2.com/products/new> (logged in).
**Note:** G2.com and **G2 Digital Markets** (Capterra/GetApp/Software Advice — see `docs/SUBMIT_G2DM.md`) are **separate stacks** even though G2 acquired the Gartner trio (sale closed 2026-02-05). Do both; they don't share a listing.

> ⚠️ **DECISION FLAG 1 — account identity.** Create the G2 account with a **personal LinkedIn** OR a **person-named `@algovault.com` email** (e.g. a real first-name alias) — **NOT** `admin@` / `info@` (those draw vetting scrutiny). An SMS-capable phone is reportedly requested. The **public** product profile stays brand-only; the account is the private verifier. **Needs Mr.1's OK** (same flag as Crunchbase).

---

## Why G2 (probe results, 2026-06-05)
- **B2B AI-recommendation surface:** G2 review pages are heavily cited by B2B buyers and by LLM assistants for "best X for Y" queries; a free listing is live + searchable **with zero reviews**.
- **Free tier includes:** profile, category placement, screenshots, a Q&A section, a **non-incentivized** review landing page, and the "Users Love Us" badge at 20 reviews ≥ 4.0★.
- **Eligibility:** B2B only; a legally-registered entity; the product website must be reachable with public pricing/about/privacy pages (all live at algovault.com).

## Pre-flight (confirm before starting)
1. **No existing listing** — search <https://www.g2.com/search?query=AlgoVault>. If an auto/seed listing exists, **claim** it instead.
2. **Live support pages** present: pricing, about, privacy (G2 checks the site is real + reachable).
3. **Category target** — G2 requires ≥1 category. Probe where comparable **B2B crypto/AI developer tools** sit and read that category's inclusion criteria. Candidate categories (confirm against the live, JS-gated category picker at submit time — **UNVERIFIED until execution**): `Cryptocurrency`, `Algorithmic Trading`, `AI/ML` developer-tooling, or an `API`/`Data` category. Pick the **one** with the closest peer set; add secondaries if offered.
4. **Assets:** logo ≥ 400px; 3–5 screenshots (track-record/verify pages + a tool response render).

---

## Approved copy (paste-ready — first line = the canonical entity-first opener; third-person; "call" not "signal")

**Product name:** `AlgoVault` &nbsp;|&nbsp; **URL:** `https://algovault.com`

**Overview** (≤ 500 words — paste into my.G2 once the listing is approved):
```
AlgoVault is an MCP (Model Context Protocol) server and API that returns a single composite trade call — direction, confidence, and market regime — for AI trading agents across cryptocurrency perpetual-futures venues. Instead of returning raw indicators for a human to interpret, it combines multiple quantitative factors into one verdict per call, alongside market-regime classification and cross-venue funding-rate comparison.

AlgoVault is built for autonomous software clients rather than human chart-readers. Agents connect over remote HTTPS using Streamable HTTP, as a local stdio process, or through MCP registries, and call a small set of tools that each return a structured response. The published track record is verifiable: every trade call is recorded on-chain, Merkle-anchored on the Base network, so results can be checked independently rather than taken on trust.

The product is aimed at developers building AI trading agents, algorithmic-trading systems, and MCP-compatible clients. Coverage spans multiple cryptocurrency derivatives venues and a broad set of assets and timeframes. A free tier provides a monthly call allowance with HOLD calls always free; usage-based paid plans serve higher volumes, and per-call micropayments are also supported.

Typical uses include adding a directional view and confidence score to an agent's decision loop, classifying the current market regime before sizing a position, and scanning cross-venue funding-rate differences. Documentation and integration examples are maintained for common MCP clients and agent frameworks.
```

---

## Click-by-click (Mr.1)
1. Create/sign in to the G2 account (decision flag 1 — personal LinkedIn or person-named `@algovault.com`, **not** `admin@`).
2. Open **<https://www.g2.com/products/new>**.
3. Enter product name **`AlgoVault`** (exact), URL `https://algovault.com`, and a short description (first paragraph of the Overview above).
4. If a **"serve as admin / I work here"** checkbox is offered, **tick it** (pre-claims the listing, saving the separate claim step). *(Field presence UNVERIFIED — JS-gated; confirm at submit.)*
5. Submit → **wait 3–5 business days** for research-team vetting.
6. Once live, complete the profile in **my.G2**: paste the full **Overview**, pick the category (pre-flight #3), upload logo + 3–5 screenshots, fill the Q&A.
7. Tell Code the live `g2.com/products/<slug>` URL → Code flips `g2` in `landing/_jsonld/entity-urls.json` + re-runs the generator.

## Review collection (LAW — genuine only)
- Use the **free, non-incentivized** review landing page. Share it **privately** with real users (e.g. Telegram-bot subscribers who genuinely use it), with a **neutral ask** ("good or bad, leave an honest review"), **paced over time**.
- **NEVER** public-blast the review link (attracts gift-card farmers), **NEVER** segment to positive-only users, **NEVER** post employee reviews. No incentivized or off-platform reviews, ever. Badge ("Users Love Us") only accrues from real reviews at 20 × ≥4.0★.

## Pushback to expect
- **Rejections:** B2C / duplicate / "plugin of another product" / unreachable website. None apply — AlgoVault is a standalone B2B API with a live site.
- **Category mismatch:** if rejected on category, re-submit to the closest peer category (pre-flight #3) and cite 2–3 comparable B2B listings.
- **Claim vs create:** if a seed listing already exists, claim it (don't duplicate).
