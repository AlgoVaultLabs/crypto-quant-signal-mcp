{
  "_doc": "OPS-CLAUDEMD-CLAIM-VERIFIER-W1 — config for scripts/check-claudemd-claims.mjs, the verifier for the vault CLAUDE.md's machine-checkable assertions. Every exemption row carries its OWN `reason`: an exemption that lives only in prose gets 'fixed' by a future wave enforcing the contract. Missing or unparseable ⇒ CLAUDEMD_CLAIMS_VERDICT=INDETERMINATE, exit 3 — never a pass.",

  "_corpus": "The corpus is the vault CLAUDE.md (private, outside this repo). CI runners cannot read it, so the gate verifies the committed claim lock (ops/claudemd-claims.lock.json) — identifiers only, never the manual's prose. When the corpus IS reachable (operator Mac), the gate re-extracts fresh, enforces lock freshness by corpus sha256, and additionally verifies the local-only classes (vault paths, home paths). The manual's text never enters this public repo.",

  "_step0_measurement": "R1 measured 2026-08-02 on corpus sha 50920a2d…: 911 backtick spans; 22 _( … )_ correction blocks (8% of corpus chars) containing 60 spans. Raw would-fire on path classes WITHOUT the correction-block strip: 14 distinct (17 sites); WITH strip: 12 distinct. After the three measured classifier refinements (bare-directory spans are prose not claims; dist/** is build output never tracked by construction; absence/deprecated markers invert or skip the check), LIVE false positives on the blocking classes = 0, and the strip alone accounts for the historical `dispatch_schedule.py` + `dist/lib/x402.js` + paths-ignore-glob citations. Ladder assignments below follow that measurement, not intuition.",

  "classes": {
    "repo-path": { "ship": "block", "note": "Backticked concrete repo file (known prefix + extension, no glob/template chars, not dist/**). Verified against git ls-files. Measured FP after refinements: 0. Highest-frequency drift class (the /app/dist/index.js and lib/check-numerical-fact-density.mjs incidents)." },
    "root-file": { "ship": "block", "note": "Bare root filenames via root_file_map (deploy.yml → .github/workflows/deploy.yml). Measured FP: 0/19." },
    "basename": { "ship": "block", "note": "Bare extension-bearing basenames resolved via the git ls-files basename join. Measured FP after template (XXXXXX) + DEPRECATED-marker refinements: 0." },
    "wiring": { "ship": "block", "note": "‘wired into X’ / ‘at `pre-push` by Y’ claims, resolved by IMPORTING check-canaries-wired.mjs's findInvocations — never a second implementation — then intersecting with wiring_point_map." },
    "script-content": { "ship": "report", "note": "Exit-code and verdict-token claims: the named script must contain the claimed exit code / token. Grep-level association — promote on the numeric criterion below." },
    "cron-schedule": { "ship": "report", "note": "`MM HH` pairs associated to a script on the same line, cross-checked against ops/monitoring/monitoring-inventory.json schedule (the committed SoT the daily reconciler asserts against the host — this gate never re-implements the host probe). Host state is operator-mutable → report." },
    "env-var": { "ship": "report", "note": "NAME= tokens: the NAME must appear in ≥1 tracked file. Weak association → report." },
    "absence": { "ship": "report", "note": "Claims marked DOES-NOT-EXIST/never-built/retired verify the INVERSE (must NOT exist). Precise but new — promote on the numeric criterion." },
    "vault-path": { "ship": "report", "local_only": true, "note": "Vault files (status.md, system-map.md, Claude files/…). Checked only when the vault is reachable; UNREACHABLE in CI is distinct from MISSING and never fires. First run found 3 true positives (all moved files)." },
    "host-path": { "ship": "report", "local_only": true, "note": "/opt|/var|/etc paths. Probed read-only ONLY under --probe-hosts; default UNPROBED. /app/** is container-scoped and never probed. Never committed to the lock." },
    "home-path": { "ship": "report", "local_only": true, "note": "~/… paths, existence-only (never read contents — admin.env is a secret store)." },
    "package-version": { "ship": "skip", "note": "MEASURED EMPTY: the corpus's package mentions are historical pins (@x402/core 2.9.0→2.20.0, inside a correction block) or non-npm CLIs (mcp-publisher, caddy) — zero live npm-dependency claims to verify. Class deliberately unimplemented until one exists; revisit then, per the prompt's ladder." },
    "glob": { "ship": "skip", "note": "Glob patterns are pattern REASONING, not file claims — the deploy.yml paths-ignore lesson: the enumerated list was deleted in favour of pointing at the SoT. Enforcing globs would re-create that duplication." },
    "prose": { "ship": "skip", "note": "Spans with whitespace (commands), template placeholders (<…>, {NEXT}, X.Y.Z, XXXXXX), bare directory references, and bare symbol tokens. Measured 720/911 spans; verifying symbols would need an AST join with unbounded FP surface — out of scope v1." }
  },

  "report_class_promotion": {
    "runs_required": 30,
    "false_positives_allowed": 0,
    "criterion": "A report class promotes to block only after ≥30 consecutive --check runs (CI + local combined) with ZERO false positives in that class, counted from the gate's own printed per-claim output — a number that can be checked, not a vibe. The flip is a config edit owned by OPS-CLAUDEMD-CLAIM-PROMOTE-W{NEXT}; do not flip it inline."
  },

  "_freshness_doc": "OPS-CLAUDEMD-CLAIM-FRESHNESS-SEVERITY-W1 CH2. A stale lock is BOOKKEEPING and REPORTS; the dangerous condition — a claim this lock recorded as VERIFIED whose path is now absent — still BLOCKS, and is now reachable WHILE the lock is stale, which it was not before. These four severities are DERIVED from buildLock's own three-state path marking, never re-derived. NOTE the distinct key: `report_class_promotion` above governs CLAIM CLASSES (repo-path, script-content, …) and is owned by OPS-CLAUDEMD-CLAIM-PROMOTE-W{NEXT}; `freshness_promotion` below governs STALENESS SEVERITY and is owned by OPS-CLAUDEMD-FRESHNESS-PROMOTE-W{NEXT}. Two criteria, two owners — do not collapse them.",

  "freshness_severity": [
    {
      "severity": "STALE_SYNCABLE",
      "ship": "report",
      "reason": "The added claim's subject resolves in this tree, so the lock simply has not absorbed it yet. Nothing about the repo is wrong and the pusher can fix it in one command. Blocking here is pure bookkeeping tax, and a gate that refuses a correct push gets disabled."
    },
    {
      "severity": "STALE_IN_FLIGHT",
      "ship": "report",
      "reason": "The subject is on a pushed remote ref but not merged. Another wave's merge order must never break this push — the same rule the per-claim in_flight downgrade already applies. Measured 2026-08-08: this was the ACTUAL state of the claim that blocked every worktree, and the severity table that shipped without this row could not classify it."
    },
    {
      "severity": "STALE_UNPUBLISHED",
      "ship": "report",
      "reason": "The subject is on no remote ref — it is in another session's working tree. The pusher can neither verify it nor honestly declare it; only the owning session can. Blocking lands the verdict on someone with no move available, which is the deadlock CLAUDE.md records having already fired three times."
    },
    {
      "severity": "STALE_DROPPED",
      "ship": "report",
      "reason": "CLAUDE.md stopped making a claim the lock still carries. The manual retiring a prescription is normal editing, not a repo defect, and the claim is no longer enforced against anything. A file DELETION is a different condition and is caught by the was_verified BLOCK, which is unaffected by this row."
    }
  ],

  "freshness_promotion": {
    "runs_required": 20,
    "escalate_after": "2026-10-31",
    "owner": "OPS-CLAUDEMD-FRESHNESS-PROMOTE-W{NEXT}",
    "reason": "STALE_SYNCABLE is the only severity that could ever justify blocking again, since it is the only one the pusher can act on. Both bounds are required: a numeric bar alone can never fire if staleness does not heal, and CLAUDE.md records a guard sitting in REPORT indefinitely as decoration with the same shape as a months-old MANUAL_PENDING.",
    "criterion": "Promote STALE_SYNCABLE back to block only after ≥20 consecutive --check runs recording zero STALE_SYNCABLE rows, measured from the shared $GIT_COMMON_DIR/algovault-claudemd-freshness.log ledger this gate appends on every run — a rate, not a guess. Past escalate_after, a still-REPORT gate must be ESCALATED to the architect with the measured healing rate attached, not silently left in place. STALE_IN_FLIGHT / STALE_UNPUBLISHED / STALE_DROPPED are NOT promotion candidates: none of them is the pusher's to fix."
  },

  "absence_markers": ["DOES NOT EXIST", "does not exist", "never built", "NEVER built", "retired by", "retired 2", "— retired", "RETIRED"],
  "nonclaim_markers": ["DEPRECATED", "deprecated"],

  "repo_prefixes": [".github/", "scripts/", "src/", "tests/", "ops/", "docs/", "audits/", "landing/", "examples/", "deploy/"],
  "_repo_prefixes_note": "hooks/ is deliberately absent: the corpus's `hooks/pre-push` refers to the AOE repo's hook, and cqsm tracks no hooks/ (measured) — including it would be a permanent false positive. dist/ is absent because build output is never tracked by construction (the tsc-emits-per-module law explains why greps there mislead).",

  "root_file_map": {
    "deploy.yml": ".github/workflows/deploy.yml",
    "Dockerfile": "Dockerfile",
    "package.json": "package.json",
    "package-lock.json": "package-lock.json",
    "server.json": "server.json",
    "manifest.json": "manifest.json",
    "lobehub-manifest.json": "lobehub-manifest.json",
    "Caddyfile": "Caddyfile",
    "CHANGELOG.md": "CHANGELOG.md",
    "README.md": "README.md",
    "tsconfig.json": "tsconfig.json"
  },

  "vault_names": ["status.md", "system-map.md", "Design.md", "XPost.md", "trim-status-config.md", "Marketing.md", "brand-facts.md", "AlgoVault-MCP-roadmap.md", "NPM-readme.md", "defensive-reductions-to-revisit.md", "algovault-automation-guide.md", "Autonomous-Optimization-Engine.md"],
  "vault_prefixes": ["Claude files/", "experiments/", "Prompt/", "Old Status/", "skills/", "research/"],

  "wiring_point_map": {
    "deploy.yml": [".github/workflows/deploy.yml"],
    "prepublishOnly": ["package.json"],
    "pre-push": ["scripts/install_", "scripts/check_test_baseline.sh", "tests/"],
    "pre-commit": ["scripts/install_", "scripts/check-author-identity.sh"],
    "package.json": ["package.json"],
    "monitoring-inventory": ["ops/monitoring/monitoring-inventory.json"]
  },
  "_wiring_point_map_note": "Entries ending in / or _ are PREFIXES. pre-push resolves through the per-gate hook installers (scripts/install_*_hook.sh — measured: session-drift and source-greppable each ship their own), the baseline gate script, or the vitest suite the pre-push hook runs (tests/). pre-commit was the LITERAL ['scripts/install_system_map_hook.sh'] until OPS-GIT-IDENTITY-CANONICALIZE-W1, when a second pre-commit gate (author-identity) shipped and a CLAUDE.md line prescribing it would have failed the BLOCKING `wiring` class purely because the map named one installer by hand. Widened to the same `scripts/install_` PREFIX convention pre-push already used — which still covers install_system_map_hook.sh — plus the gate script itself. A hand-maintained literal list of installers is the duplicated fact that goes stale; the prefix is the SoT.",

  "_corpus_enumeration": {
    "_doc": "OPS-CLAIM-VERIFIER-COVERAGE-W1 R1 — every PRESCRIPTIVE doc assessed for inclusion, with the measured firing rate that decided it. Historical sources (status.md, Old Status/**, audits/**, and any _( … )_ note) are excluded by definition: their entries are past-tense by design and must never be 'corrected' to match present reality.",
    "_measured_2026_08_02": "W1's `_( … )_` correction convention does NOT generalise — ZERO correction blocks in EVERY sibling corpus measured. So the strip is a no-op there, and the dominant false-positive class is entirely different: CROSS-REPO and EXTERNAL artifacts (a TS repo's doc citing capabilities.py, an upstream registry's openapi.yaml, another host process's env var). That is why no sibling corpus inherits CLAUDE.md's blocking classes on the strength of W1's measurement.",
    "in": [
      { "corpus": "CLAUDE.md", "state": "verified (blocking classes live)", "reason": "The prescriptive SoT every wave spec derives from. Measured 0 FPs on blocking classes (W1), re-confirmed here." }
    ],
    "out": [
      { "corpus": "Claude files/monitoring-runbook.md", "state": "duplications DELETED; not added as a verified corpus", "reason": "Measured: after R2 removed its stale restatements, every remaining machine-checkable mention is a runtime state dir, a cache dir, a mode-600 SECRET (autopilot-pg-creds — which the reconciler's own ORPHAN scan excludes as 'must never be proposed for commit'), or a cross-repo path — a ~100% FP profile for the coverage rule. The artifacts that DO matter already have monitoring-inventory rows plus DOC_PATH_CLAIM existence checks. Verifying it would be a gate that cries wolf; deleting the duplication is the durable fix and shrinks what can drift." },
      { "corpus": "Design.md", "state": "MEASURED, correction DEFERRED", "reason": "Highest-value candidate found: 15 claims, 5 firing, and all 5 verified as TRUE positives (0 FP) — 2 stale renames (check_caddy_api_route_parity.sh → live check-caddy-route-parity.mjs; tests/baseline-failures-quarantine.json → live audits/test-baseline-known-failures.txt) and 3 gates prescribed but never built. The never-built three need a build-or-retire decision that is the architect's, not this wave's — same shape as W1's numerical-fact-density finding. Owner: OPS-DESIGNMD-CLAIM-RECONCILE-W{NEXT}." },
      { "corpus": "trim-status-config.md", "state": "out", "reason": "Measured 0 extractable claims — it prescribes KEEP/ARCHIVE prose rules, not artifacts. Nothing to verify." },
      { "corpus": "research/algovault-automation-guide.md", "state": "out", "reason": "Measured 4 claims, 0 firing. A research/reference guide rather than an execution SoT; no wave derives ACs from it." },
      { "corpus": "docs/RUNBOOK-*.md (repo)", "state": "out", "reason": "Sampled 3: PARALLEL-SESSIONS 4/0, POSTGRES-MAINT 2/0, FEATURE-REGISTRY 15/2 — and both FEATURE-REGISTRY firings are cross-repo Python (capabilities.py, tests/test_capabilities_parity.py) in a TypeScript repo, i.e. the measured FP class. Low claim density, no blocking case." }
    ],
    "promotion_criterion": "A corpus moves from `out` to verified only with its own measurement showing ZERO false positives on the classes it would block, plus an exemption list for its cross-repo/external artifacts carrying a reason per row. Re-measure with `node scripts/check-claudemd-claims.mjs --measure`; do not inherit CLAUDE.md's rate."
  },

  "in_flight_claims": [],
  "_in_flight_claims_history": "Held ONE row on 2026-08-02: `ops/cron/checkout-parity.sh`, owed by OPS-HOST-DEPLOY-PROVENANCE-ROLLOUT-W1, whose files were still local to a concurrent session's worktree — absent from the working tree, origin/main, and every pushed branch. That wave landed the same day, the self-cleaning check FAILED with 'the claim now RESOLVES … DELETE this row', and the row was deleted. Recorded because the mechanism's whole point is that a declaration cannot outlive its race, and this is the proof it does not.",
  "_in_flight_semantics": "A declared in-flight claim downgrades BLOCK → REVIEW for that ONE claim, never for its class. It is self-cleaning: once the claim resolves, the gate FAILS until the row is removed, so a declaration cannot outlive the race it describes. Adding a row is a deliberate reviewed act — it is not a way to silence a real finding, and every row names the wave that owes the file.",

  "exemptions": [
    {
      "value": "LABEL=",
      "class": "env-var",
      "reason": "Redaction-STRUCTURE example in the secret-redaction law (‘mask everything after LABEL=’) — a placeholder, not an environment variable any script reads."
    },
    {
      "value": "endpoint-truth.md",
      "class": "vault-path",
      "reason": "Per-wave OUTPUT template name (waves produce audits/<wave>-endpoint-truth.md in this repo — several exist); there is deliberately no vault-root file of that literal name."
    },
    {
      "value": "hashnode_runbook.mjs",
      "class": "basename",
      "reason": "Editorial-host artifact (/opt/algovault-editorial) cited in the Hashnode-DEPRECATED law; it never had a committed ancestor in this repo, and the deprecation context makes it a status claim, not an existence claim."
    },
    {
      "value": "17 7",
      "class": "cron-schedule",
      "reason": "aoe-1 instance of monitoring-inventory-reconcile.py. Live-verified 2026-08-02 on the aoe host crontab (`17 7 * * * MONITORING_HOST_LABELS=aoe-1 …`); its inventory row lives in the AOE repo's vendored copy (OPS-AOE-MONITORING-PARITY-W1), so this repo's inventory legitimately cannot confirm it."
    }
  ]
}
