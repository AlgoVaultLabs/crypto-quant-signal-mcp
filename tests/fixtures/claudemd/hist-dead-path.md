# fixture: the SAME dead path, but inside a historical _( … )_ correction block — MUST NOT fire

The invariant is enforced at the generator now. _(Corrected 2026-01-01 FIXTURE-WAVE-W1 — the prior
line cited `scripts/definitely-not-a-real-gate.mjs` (deleted that day), which no longer exists; the
citation is kept deliberately so the history stays intact.)_
