// Inline-script-safe: markup emitted inside a JSON blob, so the less-than sign is escaped.
export const BILLING_WIDGET = '\u003cform action="/billing/portal" method="post">\u003cbutton>Manage billing\u003c/button>\u003c/form>';
