{
  "verdict": "FAIL",
  "has": [
    "✗ src/lib/pages\\.ts:2 POST /upgrade(\\s|$)"
  ],
  "lacks": [],
  "finding": "Mount prefixes are not composed: a nested router (`app.use('/v1', v1); v1.use('/billing', billing)`) or a sub-app (`const billing = express()`) is mapped to the wrong path, so a hop into it counts as terminal",
  "why": "round-3 FAIL-OPEN reproduction: v3 printed PASS while the code as written is refused by form-action in Chrome/Safari"
}
