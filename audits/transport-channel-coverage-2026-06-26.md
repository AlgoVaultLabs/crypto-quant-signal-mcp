# Transport Channel Coverage Audit — 2026-06-26

**Wave:** TRANSPORT-CHANNEL-COVERAGE-AUDIT-W1 · **Target ICP:** META
**Verdict:** ✅ GREEN — all four transports in lockstep with the feature-registry SoT. Zero coverage gaps. No additive `channels{}` rows required. No gate/price/quota mutation → no Plan-Mode.
**Anchors derived from:** `origin/main` @ `dd26e8f` (worktree `cqsm-wt-transport-coverage-audit`), live `api.algovault.com` (deploy v1.20.1), bot repo `~/algovault-bot`, Hetzner `204.168.185.24`.

This is the VERIFICATION + completeness pass FEATURE-REGISTRY-SOT-W1 was missing: a single live confirmation that every tool's declared channels match what each transport actually serves.

---

## 1. Registry enumeration (SoT — `src/lib/feature-registry.ts`)

`FEATURE_REGISTRY` = **8 canonical tools + 1 alias** (`get_trade_signal` → `get_trade_call`) = **9 callable names**. Helpers confirmed present and load-bearing: `getFeature` (name/alias resolve), `allToolNames` (canonical+aliases, drives MCP loop), `projectCapabilities` (public projection for `/capabilities`), `webhookEventTypes` (drives `VALID_EVENTS`).

## 2. Coverage matrix — `declared (registry)` / `live-served`

Generated by `scripts/transport-coverage-matrix.mjs --live https://api.algovault.com` (re-runnable each release). ✓ = present, — = absent.

| Tool (canonical) | aliases | mcp | httpX402 | bot | webhook | webhookEvent | quota | x402 |
|---|---|---|---|---|---|---|---|---|
| `get_trade_call` | `get_trade_signal` | ✓ / ✓ | ✓ / ✓ | ✓ / ✓ | ✓ / ✓ | trade_call | per-non-hold | $0.02 |
| `get_market_regime` | — | ✓ / ✓ | ✓ / ✓ | ✓ / ✓ | ✓ / ✓ | regime_shift | per-call | $0.02 |
| `scan_funding_arb` | — | ✓ / ✓ | ✓ / ✓ | ✓ / ✓ | — / — | — | per-call | $0.01 |
| `scan_trade_calls` | — | ✓ / ✓ | ✓ / ✓ | ✓ / ✓ | ✓ / ✓ | scan_digest | per-non-hold-min1 | $0.02 |
| `get_equity_call` | — | ✓ / ✓ | ✓ / ✓ | — / — | — / — | — | per-non-hold | $0.02 |
| `get_equity_regime` | — | ✓ / ✓ | ✓ / ✓ | — / — | — / — | — | per-call | $0.02 |
| `chat_knowledge` | — | ✓ / ✓ | — / — | — / — | — / — | — | rate-limited | — |
| `search_knowledge` | — | ✓ / ✓ | — / — | — / — | — / — | — | rate-limited | — |

**Every cell is `✓/✓` or `—/—` — registry == live on all four channels, for every tool. Zero undocumented live tools; zero registry tools missing from a channel they declare.**

### Per-channel live evidence

- **MCP** — `tools/list` (3-step streamable-HTTP handshake) returned **9 names** == `allToolNames()` == `/capabilities` names, byte-for-byte. (No `Mcp-Session-Id` header returned — confirms the stateless transport, `sessionIdGenerator: undefined`, as designed.)
- **HTTP x402** — see §3.
- **Bot** — see §4.
- **Webhook** — `VALID_EVENTS` derives from `webhookEventTypes()` = `[trade_call, regime_shift, scan_digest]` (3 events). Static parity = **true** (canary assertion 5). `scan_funding_arb` correctly carries no `webhookEvent` (webhook=false).

## 3. Paid-HTTP-tool count — AUTHORITATIVE: **6**

Resolves the residual "3 vs 6" question: the OPS-X402-PRICING-EXPANSION-W1 3→6 expansion is **live**.

- `HTTP_TOOLS` (gated + Bazaar-discoverable) = **6**: `get_trade_signal`, `scan_funding_arb`, `get_market_regime`, `scan_trade_calls`, `get_equity_call`, `get_equity_regime`.
- Registry `httpX402 && x402` canonical set = 6; `HTTP_TOOLS` alias-resolved → canonical == registry set: **true** (canary assertion 3).
- The registry declares the trade-call route under canonical `get_trade_call`, but `HTTP_TOOLS` deliberately keys on the back-compat alias `get_trade_signal` (Cowork A2, 2026-05-29) so the gated/discoverable HTTP name + x402-shape stay byte-identical. The canary special-cases this (resolves alias→canonical before comparing).

### Live `/x402/*` route probes (GET → 402 challenge, price from x402 **v2** `accepts[0].amount`)

| Route | HTTP | price | network | payTo |
|---|---|---|---|---|
| `/x402/get_trade_signal` | 402 | $0.02 | eip155:8453 (Base) | 0x778A…7d59 |
| `/x402/scan_funding_arb` | 402 | $0.01 | eip155:8453 | 0x778A…7d59 |
| `/x402/get_market_regime` | 402 | $0.02 | eip155:8453 | 0x778A…7d59 |
| `/x402/scan_trade_calls` | 402 | $0.02 | eip155:8453 | 0x778A…7d59 |
| `/x402/get_equity_call` | 402 | $0.02 | eip155:8453 | 0x778A…7d59 |
| `/x402/get_equity_regime` | 402 | $0.02 | eip155:8453 | 0x778A…7d59 |
| `/x402/get_trade_call` (paid alias) | 402 | $0.02 | eip155:8453 | 0x778A…7d59 |

All live prices == registry `x402.basePriceUsd`. `payTo` = the canonical Rabby receive wallet (`0x778A05280Fd8dB980E920fE9f31d0A8eAbD17d59`), asset = USDC-on-Base (`0x833589fC…2913`).

**`get_trade_call` alias confirmed:** route exists, 402s at $0.02, serves its own resource URL (`/x402/get_trade_call`), delegates to the `get_trade_signal` producer, is NOT in `HTTP_TOOLS`, and is sent `discoverable:false` — so the "free on MCP + non-discoverable" invariant (A2/A3) holds. Control: non-`httpX402` tools (`chat_knowledge`, `search_knowledge`) correctly return **404** (route never mounted).

## 4. Bot surface (consumer of `GET /capabilities`)

- `capabilities.fetch_capabilities()` → `GET /capabilities` is the live tool-surface source, 3-tier fail-open: **live → committed `capabilities-fallback.json` → `BOT_TOOL_SURFACE`** (never empty).
- `BOT_TOOL_SURFACE` keys = the **4** `channels.bot==true` tools: `get_trade_call`, `get_market_regime`, `scan_trade_calls`, `scan_funding_arb`. `unmapped_bot_tools(live)` = **∅** (CH5 canary requires empty).
- Committed `capabilities-fallback.json` bot-flagged set == live `/capabilities` bot set == `BOT_TOOL_SURFACE` (verified by diff).
- `validators.ALERT_TYPES` derives from `BOT_TOOL_SURFACE` (`kind=="alert"`) = `{calls, regime, both}`.
- The bot's command UX (`/call /scan /regime /funding /watch`, wizards, buttons) is bespoke BY DESIGN and out of scope — only the tool SURFACE must derive, and it does.

## 5. `/capabilities` public-safe assertion

- Per-tool keys present: `canonical, channels, description, enabled, name, quota, x402` — **zero forbidden fields** (no `descriptionRef`, `outcome_return_pct`, `outcome_price`, `eligible_non_hold`, no handler refs).
- `Cache-Control: public, max-age=60, must-revalidate` ✓.
- Allow-list projection holds Data Integrity (THE LAW): no Phase-E / outcome WR surfaced.

## 6. Canary (`scripts/check-feature-registry-drift.mjs`)

| Mode | Command | rc | Result |
|---|---|---|---|
| STATIC | `--check` (dist) | **0** | `9 tools, 6 gated x402 routes, 3 webhook events, projection clean` |
| LIVE | `--live https://api.algovault.com` | **0** | `9 tools match across MCP + /capabilities + x402` |

**Weekly host cron (Hetzner `204.168.185.24`):** `37 5 * * 1` (Mon 05:37 UTC) — `crontab`, not a systemd timer (live reality over the spec's "systemctl list-timers" framing). Last run **Jun 22 05:37 UTC**, log = `LIVE in-sync ✅ — 9 tools match`, **no drift fire**. Log: `/var/log/algovault-monitoring-feature-registry-drift.log`. (Deploy dir `/opt/crypto-quant-signal-mcp` @ `71ca651`, 1 landing-only commit behind `origin/main` — registry unaffected; live `/capabilities` serves v1.20.1.)

---

## Findings & gap handling

**No coverage gaps.** No additive `channels{}` rows needed; the registry already covers every live tool on every channel and both canary modes are green — the valid, complete "clean bill of health" outcome the wave allows for.

### Finding F1 (filed, NOT mutated) — canary `--live` price sub-check is a no-op vs the x402 **v2** body shape

`check-feature-registry-drift.mjs` LIVE assertion B reads `body?.accepts?.[0]?.maxAmountRequired`, but the live 402 challenge is x402 **v2** (`x402Version: 2`) and carries the price in `accepts[0].amount`. So `maxAmountRequired` is `undefined` → the `if (atomic !== undefined)` guard skips the live price comparison silently. The 402-**status** check and route-set parity still work, and `--check` assertion 2 still locks registry↔`TOOL_PRICING` statically, so **registry↔code price parity is enforced** — only **live-served price drift** is currently undetected by the weekly canary.

- **Risk class:** verification robustness (latent), NOT a gate/price/quota mutation and NOT a coverage gap. Live prices are all correct today (§3).
- **Mitigation shipped this wave:** `scripts/transport-coverage-matrix.mjs` reads `accepts[0].amount` (v2, with v1 `maxAmountRequired` fallback) and surfaces live prices, so re-running it each release covers the gap.
- **Filed:** `OPS-X402-CANARY-PRICE-FIELD-W1` (see `audits/OPS-X402-CANARY-PRICE-FIELD-W1.md`) — one-line additive field-name fix to the canary; touches no gate/price/quota/response-shape. Handed to architect for ratification (a verification-script edit is outside this audit's additive `channels{}`-only write scope).

---

## Acceptance criteria

- [x] `node scripts/check-feature-registry-drift.mjs --live` exits **rc=0**.
- [x] Coverage matrix shows registry == live on all four channels for every tool; zero undocumented live tools; zero registry tools missing from a declared channel.
- [x] Paid-HTTP-tool count stated authoritatively (**6**) with live `/capabilities` + `/x402/*` 402 evidence; `get_trade_call` alias confirmed (paid, delegates to `get_trade_signal`, absent from `HTTP_TOOLS`).
- [x] `/capabilities` confirmed public-safe (no internal fields).
- [x] `audits/transport-channel-coverage-2026-06-26.md` written; `scripts/transport-coverage-matrix.mjs` shipped; `status.md` appended.

## Reproduce

```
git -C ~/code/crypto-quant-signal-mcp fetch origin
git worktree add ../cqsm-wt-transport-coverage-audit origin/main && cd ../cqsm-wt-transport-coverage-audit
ln -s ~/code/crypto-quant-signal-mcp/node_modules ./node_modules && npm run build
node scripts/check-feature-registry-drift.mjs --check
node scripts/check-feature-registry-drift.mjs --live https://api.algovault.com
node scripts/transport-coverage-matrix.mjs --live https://api.algovault.com --md audits/transport-channel-coverage-$(date -u +%F).md
```
